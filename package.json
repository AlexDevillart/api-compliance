{
  "name": "api-server",
  "version": "1.0.0",
  "private": true,
  "description": "API para processar questionário anticorrupção (compatível com Supabase Edge Function).",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "nodemailer": "^6.9.15"
  },
  "engines": {
    "node": ">=17.4.0"
  }
}
